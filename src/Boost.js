import React from 'react'

const Boost = ({onClick}) => {
  
  return (
    <div onClick={onClick}>
        <button>
            Colapsar Iguales
        </button>
    </div>
  )
}

export default Boost