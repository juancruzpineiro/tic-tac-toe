import React from 'react'

const Maximo = ({onClick}) => {
  
  return (
    <div onClick={onClick}>
        <button>
            Movida Maxima
        </button>
    </div>
  )
}

export default Maximo